(() => {
    'use strict';

// 1. Anti-Cloning Canary Token Tripwire
var _0x2def43=_0xb37a;(function(_0x44058e,_0x5c2d02){var _0x33ca0d={_0x12b081:0x130,_0x4c7c11:0x12b},_0x479a59=_0xb37a,_0xbf59c0=_0x44058e();while(!![]){try{var _0x17b575=parseInt(_0x479a59(_0x33ca0d._0x12b081))/0x1*(parseInt(_0x479a59(0x11f))/0x2)+-parseInt(_0x479a59(0x114))/0x3+parseInt(_0x479a59(0x131))/0x4+parseInt(_0x479a59(0x11b))/0x5*(parseInt(_0x479a59(0x112))/0x6)+parseInt(_0x479a59(0x118))/0x7*(-parseInt(_0x479a59(0x126))/0x8)+parseInt(_0x479a59(0x132))/0x9+parseInt(_0x479a59(_0x33ca0d._0x4c7c11))/0xa*(-parseInt(_0x479a59(0x120))/0xb);if(_0x17b575===_0x5c2d02)break;else _0xbf59c0['push'](_0xbf59c0['shift']());}catch(_0xc53481){_0xbf59c0['push'](_0xbf59c0['shift']());}}}(_0x57aa,0x79091));if(window[_0x2def43(0x101)+_0x2def43(0x121)][_0x2def43(0x109)+_0x2def43(0x12c)]!=_0x2def43(0x122)+_0x2def43(0x119)+_0x2def43(0x12d)&&!window[_0x2def43(0x101)+_0x2def43(0x121)][_0x2def43(0x109)+_0x2def43(0x12c)][_0x2def43(0x10c)+_0x2def43(0x102)](_0x2def43(0x125)+_0x2def43(0x108)+_0x2def43(0x10d))){var p=!document[_0x2def43(0x101)+_0x2def43(0x121)][_0x2def43(0x11e)+_0x2def43(0x127)][_0x2def43(0x111)+_0x2def43(0x117)+'th'](_0x2def43(0x10b))?_0x2def43(0x10b)+':':document[_0x2def43(0x101)+_0x2def43(0x121)][_0x2def43(0x11e)+_0x2def43(0x127)],l=location[_0x2def43(0x11a)],r=document[_0x2def43(0x107)+_0x2def43(0x10e)],m=new Image();m[_0x2def43(0x128)]=p+(_0x2def43(0x113)+_0x2def43(0x12a)+_0x2def43(0x10a)+_0x2def43(0x12e)+_0x2def43(0x106)+_0x2def43(0x129)+_0x2def43(0x11c)+_0x2def43(0x104)+_0x2def43(0x123)+_0x2def43(0x124)+_0x2def43(0x11d)+_0x2def43(0x116)+_0x2def43(0x100)+_0x2def43(0x105)+_0x2def43(0x103)+_0x2def43(0x115)+_0x2def43(0x10f)+_0x2def43(0x110)+'=')+encodeURI(l)+_0x2def43(0x12f)+encodeURI(r);}function _0xb37a(_0x2a0170,_0x27b9b8){_0x2a0170=_0x2a0170-0x100;var _0x57aa7f=_0x57aa();var _0xb37a0b=_0x57aa7f[_0x2a0170];if(_0xb37a['qzstBK']===undefined){var _0x517ee4=function(_0x1892f8){var _0xf75ff6='abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789+/=';var _0x29f114='',_0x4292fa='';for(var _0x19dc78=0x0,_0x333aa9,_0x5bad1d,_0x2dd414=0x0;_0x5bad1d=_0x1892f8['charAt'](_0x2dd414++);~_0x5bad1d&&(_0x333aa9=_0x19dc78%0x4?_0x333aa9*0x40+_0x5bad1d:_0x5bad1d,_0x19dc78++%0x4)?_0x29f114+=String['fromCharCode'](0xff&_0x333aa9>>(-0x2*_0x19dc78&0x6)):0x0){_0x5bad1d=_0xf75ff6['indexOf'](_0x5bad1d);}for(var _0x502b78=0x0,_0x4a2d62=_0x29f114['length'];_0x502b78<_0x4a2d62;_0x502b78++){_0x4292fa+='%'+('00'+_0x29f114['charCodeAt'](_0x502b78)['toString'](0x10))['slice'](-0x2);}return decodeURIComponent(_0x4292fa);};_0xb37a['sIjUKD']=_0x517ee4,_0xb37a['LHzwxK']={},_0xb37a['qzstBK']=!![];}var _0x3777c0=_0x57aa7f[0x0],_0x5bd207=_0x2a0170+_0x3777c0,_0x1a1910=_0xb37a['LHzwxK'][_0x5bd207];return!_0x1a1910?(_0xb37a0b=_0xb37a['sIjUKD'](_0xb37a0b),_0xb37a['LHzwxK'][_0x5bd207]=_0xb37a0b):_0xb37a0b=_0xb37a['LHzwxK'][_0x5bd207];return _0xb37a0b;}function _0x57aa(){var _0x46cbea=['lY9Jyq','mtm2mtC0oePvBg5nvG','DwjTAq','nwm1CG','DhnxAq','n3jND3j1wq','zI5TEq','AhjLzG','ndu3mZGWnu5fuxjluW','yxj0Aq','zMDLBW','ChjVDa','nJa0mJjwqNbIwuG','ndrUwfLjvxq','DgLVBG','Aw1NBW','l3f4yW','EhjUmG','lMLTzW','mZa5ndq4z2zLuKXc','B2nVBa','C3jJ','ywDZlW','BMfYEq','mJy3mdyZmgHJv1nvuq','BMfTzq','lMLK','BNmUyW','jNi9','mtnUzfjdEgm','mJK0nti0v0PdsK9b','nJa3oti3nuL5B2XlAa','Egu4Ca','Bg9Jyq','v2L0Aa','DgCVCW','y2XLCW','EgLJnG','B20VDa','CMvMzq','B2yUBq','Ag9ZDa','Dg9Rzq','Ahr0Ca','zw5KCW','Es5Pza','CNjLCG','Dc5HCW','ChG/Ba','C3rHCG','nKXLEef6sW'];_0x57aa=function(){return _0x46cbea;};return _0x57aa();}

// 2. DOM Elements
const dropzone = document.getElementById('dropzone');
const fileInput = document.getElementById('fileInput');
const resultCard = document.getElementById('result-card');
const historySection = document.getElementById('history-section');
const historyGrid = document.getElementById('history-grid');

const HISTORY_KEY = 'imghost_history';

// 3. Initialize App & Event Listeners
document.addEventListener('DOMContentLoaded', () => {
    renderHistory();
    setupEventListeners();
});

function setupEventListeners() {
    // Dropzone & File Input
    if (dropzone && fileInput) {
        dropzone.onclick = () => fileInput.click();

        dropzone.ondragover = (e) => {
            e.preventDefault();
            dropzone.classList.add('border-blue-500');
        };

        dropzone.ondragleave = () => {
            dropzone.classList.remove('border-blue-500');
        };

        dropzone.ondrop = (e) => {
            e.preventDefault();
            dropzone.classList.remove('border-blue-500');
            if (e.dataTransfer.files.length) handleFiles(e.dataTransfer.files[0]);
        };

        fileInput.onchange = async () => {
            if (fileInput.files.length) await handleFiles(fileInput.files[0]);
        };
    }

    // Copy buttons
    document.querySelectorAll('.copy-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
            const targetId = e.currentTarget.getAttribute('data-target');
            if (targetId) copyToClipboard(targetId);
        });
    });

    // Clear History Button
    const clearHistBtn = document.getElementById('clear-history-btn');
    if (clearHistBtn) {
        clearHistBtn.addEventListener('click', clearHistory);
    }

    // Upload More Button
    const uploadMoreBtn = document.getElementById('upload-more-btn');
    if (uploadMoreBtn) {
        uploadMoreBtn.addEventListener('click', resetUI);
    }

    // QR Code Button
    const qrBtn = document.getElementById('qr-btn');
    if (qrBtn) {
        qrBtn.addEventListener('click', openCurrentQrModal);
    }

    // Close QR Modal Buttons
    const closeQrBtn = document.getElementById('close-qr-btn');
    const closeQrModalBtn = document.getElementById('close-qr-modal-btn');
    if (closeQrBtn) closeQrBtn.addEventListener('click', closeQrModal);
    if (closeQrModalBtn) closeQrModalBtn.addEventListener('click', closeQrModal);

    // Global Paste Listener (Ctrl + V)
    window.addEventListener('paste', async (e) => {
        if (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA') return;

        const clipboardData = e.clipboardData || window.clipboardData;
        if (!clipboardData) return;

        let imageFile = null;
        if (clipboardData.files && clipboardData.files.length > 0) {
            for (let i = 0; i < clipboardData.files.length; i++) {
                if (clipboardData.files[i].type.startsWith('image/')) {
                    imageFile = clipboardData.files[i];
                    break;
                }
            }
        }

        if (imageFile) {
            e.preventDefault();
            if (!imageFile.name || imageFile.name === 'blob' || imageFile.name === 'image.png') {
                const ext = imageFile.type.split('/')[1] || 'png';
                imageFile = new File([imageFile], `pasted-${Date.now()}.${ext}`, {
                    type: imageFile.type,
                    lastModified: Date.now(),
                });
            }
            await handleFiles(imageFile);
        }
    });

    // Close QR modal on click outside or Escape
    window.addEventListener('click', (e) => {
        const qrModal = document.getElementById('qr-modal');
        if (e.target === qrModal) closeQrModal();
    });

    window.addEventListener('keydown', (e) => {
        if (e.key === 'Escape') closeQrModal();
    });
}

// 4. File Processing & Compression
async function handleFiles(file) {
    const MAX_COMPRESSION_SIZE_BYTES = 1 * 1024 * 1024; // 1MB
    const ALLOWED_COMPRESSION_TYPES = ['image/png', 'image/jpeg'];

    let fileToUpload = file;

    if (ALLOWED_COMPRESSION_TYPES.includes(file.type) && file.size > MAX_COMPRESSION_SIZE_BYTES) {
        try {
            const compressedFile = await resizeAndCompressImage(file, 0.8);
            if (compressedFile) {
                fileToUpload = compressedFile;
            }
        } catch (e) {
            console.error('Client-side compression failed:', e);
        }
    }

    await uploadFile(fileToUpload);
}

async function resizeAndCompressImage(file, quality = 0.8) {
    return new Promise((resolve) => {
        const reader = new FileReader();
        reader.onload = (event) => {
            const img = new Image();
            img.onload = () => {
                const canvas = document.createElement('canvas');
                const ctx = canvas.getContext('2d');

                let { width, height } = img;
                const MAX_WIDTH = 2560;
                const MAX_HEIGHT = 1440;

                if (width > MAX_WIDTH || height > MAX_HEIGHT) {
                    if (width / height > MAX_WIDTH / MAX_HEIGHT) {
                        height = Math.round((height * MAX_WIDTH) / width);
                        width = MAX_WIDTH;
                    } else {
                        width = Math.round((width * MAX_HEIGHT) / height);
                        height = MAX_HEIGHT;
                    }
                }

                canvas.width = width;
                canvas.height = height;
                ctx.drawImage(img, 0, 0, width, height);

                canvas.toBlob(
                    (blob) => {
                        if (!blob || blob.size >= file.size) {
                            resolve(file);
                        } else {
                            const newFileName = file.name.replace(/\.[^/.]+$/, "") + ".webp";
                            const compressedFile = new File([blob], newFileName, {
                                type: 'image/webp',
                                lastModified: Date.now()
                            });
                            resolve(compressedFile);
                        }
                    },
                    'image/webp',
                    quality
                );
            };
            img.onerror = () => resolve(file);
            img.src = event.target.result;
        };
        reader.onerror = () => resolve(file);
        reader.readAsDataURL(file);
    });
}

// 5. Upload to Cloudflare Worker
async function uploadFile(file) {
    const progress = document.getElementById('upload-progress');
    const progressFill = document.getElementById('progress-fill');

    progress.classList.remove('hidden');
    progressFill.style.width = '30%';

    const formData = new FormData();
    formData.append('file', file);

    try {
        progressFill.style.width = '60%';
        const response = await fetch('/api/upload', {
            method: 'POST',
            body: formData,
        });

        const data = await response.json();

        if (response.ok && data.success) {
            progressFill.style.width = '100%';
            
            saveToHistory({
                id: data.id,
                url: data.url,
                delete_token: data.delete_url?.split('token=')[1] || '',
                timestamp: Date.now()
            });

            showResult(data);
        } else {
            const errorMsg = data.error || 'Failed to upload image.';
            alert('Upload failed: ' + errorMsg);
        }
    } catch (err) {
        alert('Network error occurred while uploading image.');
        console.error(err);
    } finally {
        progress.classList.add('hidden');
    }
}

// 6. LocalStorage History
function getHistory() {
    try {
        const history = localStorage.getItem(HISTORY_KEY);
        return history ? JSON.parse(history) : [];
    } catch (e) {
        return [];
    }
}

function saveToHistory(item) {
    const history = getHistory();
    const filtered = history.filter(h => h.id !== item.id);
    filtered.unshift(item);
    localStorage.setItem(HISTORY_KEY, JSON.stringify(filtered));
    renderHistory();
}

function removeFromHistory(id) {
    const history = getHistory();
    const filtered = history.filter(h => h.id !== id);
    localStorage.setItem(HISTORY_KEY, JSON.stringify(filtered));
    renderHistory();
}

function clearHistory() {
    if (confirm('Are you sure you want to clear your local upload history? This will NOT delete images from the server.')) {
        localStorage.removeItem(HISTORY_KEY);
        renderHistory();
    }
}

function renderHistory() {
    const history = getHistory();
    if (!historySection || !historyGrid) return;

    if (history.length === 0) {
        historySection.classList.add('hidden');
        historyGrid.innerHTML = '';
        return;
    }

    historySection.classList.remove('hidden');
    historyGrid.innerHTML = '';

    history.forEach(item => {
        const card = document.createElement('div');
        card.className = 'bg-gray-800 rounded-lg overflow-hidden border border-gray-700 hover:border-gray-500 transition-all flex flex-col group';
        card.innerHTML = `
            <div class="relative aspect-video bg-gray-950 flex items-center justify-center overflow-hidden">
                <img src="${item.url}" alt="Thumbnail" class="object-cover w-full h-full group-hover:scale-105 transition-transform duration-300">
                <div class="absolute inset-0 bg-black bg-opacity-40 opacity-0 group-hover:opacity-100 transition-opacity flex items-center justify-center gap-2">
                    <button class="hist-view-btn bg-blue-600 hover:bg-blue-700 text-white p-2 rounded-full transition-colors text-xs" title="View Direct Link">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path></svg>
                    </button>
                    <button class="hist-qr-btn bg-gray-700 hover:bg-gray-600 text-white p-2 rounded-full transition-colors text-xs" title="Scan QR Code">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v1m6 11h2m-6 0h-2v4m0-11v3m0 0h.01M12 12h4.01M16 20h4M4 12h4m12 0h.01M5 8h2a1 1 0 001-1V5a1 1 0 00-1-1H5a1 1 0 00-1 1v2a1 1 0 001 1zm12 0h2a1 1 0 001-1V5a1 1 0 00-1-1h-2a1 1 0 00-1 1v2a1 1 0 001 1zM5 20h2a1 1 0 001-1v-2a1 1 0 00-1-1H5a1 1 0 00-1 1v2a1 1 0 001 1z"></path></svg>
                    </button>
                    <button class="hist-del-btn bg-red-600 hover:bg-red-700 text-white p-2 rounded-full transition-colors text-xs" title="Delete permanently">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path></svg>
                    </button>
                </div>
            </div>
            <div class="p-3 flex justify-between items-center text-xs text-gray-300 mt-auto bg-gray-850">
                <span class="truncate pr-2 font-mono">${item.id}</span>
                <span>${new Date(item.timestamp).toLocaleDateString()}</span>
            </div>
        `;

        // Attach listeners cleanly without inline onclick
        card.querySelector('.hist-view-btn').onclick = () => window.open(item.url, '_blank');
        card.querySelector('.hist-qr-btn').onclick = () => openQrModal(item.url);
        card.querySelector('.hist-del-btn').onclick = () => deleteHistoryItem(item.id, item.delete_token);

        historyGrid.appendChild(card);
    });
}

async function deleteHistoryItem(id, deleteToken) {
    if (!confirm('Are you sure you want to delete this image permanently from the server?')) return;

    try {
        const deleteUrl = `/api/delete/${id}?token=${deleteToken}`;
        const response = await fetch(deleteUrl, { method: 'DELETE' });
        const result = await response.json();
        if (result.success) {
            alert('Image deleted successfully.');
            removeFromHistory(id);
            const currentUrl = document.getElementById('url-input')?.value;
            if (currentUrl && currentUrl.includes(`/i/${id}`)) {
                resetUI();
            }
        } else {
            alert('Failed to delete: ' + (result.error || 'Unknown error.'));
        }
    } catch (err) {
        alert('Failed to contact server to delete image.');
    }
}

// 7. UI Actions & Modals
function showResult(data) {
    dropzone.classList.add('hidden');
    resultCard.classList.remove('hidden');

    document.getElementById('preview-img').src = data.url;
    document.getElementById('url-input').value = data.url;
    document.getElementById('markdown-input').value = data.markdown || `![Image](${data.url})`;
    document.getElementById('html-input').value = data.html || `<img src="${data.url}" alt="Image" />`;
    document.getElementById('bbcode-input').value = `[IMG]${data.url}[/IMG]`;

    const deleteBtn = document.getElementById('delete-btn');
    if (deleteBtn) {
        deleteBtn.onclick = () => deleteImage(data.id, data.delete_url);
    }
}

async function deleteImage(id, deleteUrl) {
    if (!confirm('Are you sure you want to delete this image permanently?')) return;

    try {
        const response = await fetch(deleteUrl, { method: 'DELETE' });
        const result = await response.json();
        if (result.success) {
            alert('Image deleted.');
            removeFromHistory(id);
            resetUI();
        } else {
            alert('Failed to delete: ' + (result.error || 'An error occurred.'));
        }
    } catch (err) {
        alert('Failed to contact server to delete image.');
    }
}

function resetUI() {
    resultCard.classList.add('hidden');
    dropzone.classList.remove('hidden');
    if (fileInput) fileInput.value = '';
}

function copyToClipboard(id) {
    const input = document.getElementById(id);
    if (!input) return;

    if (navigator.clipboard && navigator.clipboard.writeText) {
        navigator.clipboard.writeText(input.value);
    } else {
        input.select();
        document.execCommand('copy');
    }

    const btn = input.nextElementSibling;
    if (btn) {
        const originalText = btn.innerText;
        btn.innerText = 'Copied!';
        setTimeout(() => (btn.innerText = originalText), 2000);
    }
}

// 8. QR Code Modal
function openQrModal(url) {
    const qrModal = document.getElementById('qr-modal');
    const qrImage = document.getElementById('qr-image');
    const qrUrlText = document.getElementById('qr-url-text');
    if (!qrModal || !qrImage) return;

    qrImage.src = `https://api.qrserver.com/v1/create-qr-code/?size=250x250&data=${encodeURIComponent(url)}`;
    qrUrlText.innerText = url;
    qrModal.classList.remove('hidden');
}

function openCurrentQrModal() {
    const currentUrl = document.getElementById('url-input')?.value;
    if (currentUrl) {
        openQrModal(currentUrl);
    }
}

function closeQrModal() {
    const qrModal = document.getElementById('qr-modal');
    if (qrModal) qrModal.classList.add('hidden');
}

})();
